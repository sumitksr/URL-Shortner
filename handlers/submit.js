export function handleSubmit(e, originalUrl, customShortUrl) {
  e.preventDefault();
  console.log("Original URL:", originalUrl);
  console.log("Custom Short URL:", customShortUrl);
  // Further processing logic can go here.
}